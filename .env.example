# Server-only secret used by the Vercel API. Never prefix this with VITE_.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional API override. Production uses the same-origin /api route by default.
# VITE_WORLD_API_URL=https://your-world-api.example.com
